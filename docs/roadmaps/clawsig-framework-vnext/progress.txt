## Codebase Patterns
- Build trust with signed policy + runner + reviewer artifacts, not ambient configuration or screenshots.
- Prefer fail-closed verification and marketplace gating over advisory-only reporting.
- Keep reviewer outputs honest about what is and is not proven.
- Expand assurance without regressing canonical proof-production reliability.

# Ralph Progress Log
Started: 2026-03-20T22:40:00Z
---

## 2026-03-20T22:40:00Z - Roadmap seeded (planning)
- What was implemented
  - Added roadmap folder `docs/roadmaps/clawsig-framework-vnext/` with README/prd/progress files.
  - Added Beads breakdown under `monorepo-af2` with 24 child tasks spanning reliability, signed policy, attestation, marketplace enforcement, reviewer UX, DLP, cross-runtime parity, and transparency/revocation.
  - Updated the roadmaps index to mark this as the next active framework-improvement roadmap after Privacy Assurance v1.
- Notes
  - Initial execution should stay dependency-aware: fix reliability and establish signed policy/measurement foundations before broad marketplace or transparency productization.
  - Remote attestation is treated here as delivered framework work, not hand-wavy future positioning.
---

## 2026-03-20T23:15:15Z - Wave 1 reliability lane merged
- What was implemented
  - Closed `AF2-REL-001`..`AF2-REL-004` via PR #484.
  - Repaired `work-submit` suite debt, made `wrap-sigterm` deterministic, removed unsigned canonical receipt leakage, and added release/test proof-reliability guardrails.
- Validation
  - `packages/clawverify-cli`: `npm run test:proof-reliability`
  - `packages/clawsig-sdk`: `npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
- Notes
  - A post-review CI fix switched the `packages/clawsig-sdk` guardrail install step from `npm ci` to `npm install --ignore-scripts` because the repo does not track that package lockfile.
---

## 2026-03-20T23:58:45Z - Wave 1 signed-policy lane merged
- What was implemented
  - Closed `AF2-POL-001` and `AF2-POL-002` via PR #485.
  - Added signed policy bundle schemas, deterministic org/project/task/exception policy resolution, proofed-mode policy binding metadata, and offline/service fail-closed verification parity.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-core`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/policy-binding.test.ts test/wrap-policy-binding.test.ts`
  - `packages/clawverify-cli`: `npm run test:proof-reliability`
  - `services/clawverify`: `npm run typecheck`
  - `services/clawverify`: `vitest run test/policy-binding.test.ts`
- Notes
  - Reviewer fixes required proofed-mode signed policy bundles, restored explicit effective-policy hash binding, and added service-side enforcement parity.
  - Post-review CI fixes updated `wrap-quiet` test helpers to provision signed bundles for proofed-mode guardrails and corrected service verifier helper return-shape typing.
---

## 2026-03-21T00:27:28Z - Wave 1 runner-measurement lane merged
- What was implemented
  - Closed `AF2-ATT-001` via PR #487.
  - Added deterministic proofed-run runner measurement metadata, binding it into proof-bundle output and validating it fail-closed in both offline and service verifier paths.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-core`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/policy-binding.test.ts test/wrap-policy-binding.test.ts test/wrap-sigterm.test.ts`
  - `packages/clawverify-cli`: `npm run test:proof-reliability`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `vitest run test/policy-binding.test.ts`
- Notes
  - Reviewer fixes required proofed runs to fail closed when mandatory runner assets could not be hashed and required both verifier surfaces to validate `runner_measurement` instead of treating it as opaque metadata.
  - Wave 1 foundations are now complete; attestation receipt issuance/trust-tier uplift remains for later lanes.
---

## 2026-03-21T01:00:03Z - Wave 2 richer DLP taxonomy lane merged
- What was implemented
  - Closed `AF2-DLP-001` via PR #489.
  - Expanded proofed DLP evidence from the minimal privacy-v1 classes to a richer deterministic taxonomy with custom-rule support and policy-bound verifier checks.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/wrap-quiet.test.ts`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `npm test -- test/proof-bundle-data-handling.test.ts`
  - `services/clawverify`: `npm test`
- Notes
  - Reviewer fixes tightened built-in class/rule coherence and required signed policy evidence for custom-rule matches so expanded DLP receipts stay deterministic and fail closed.
---

## 2026-03-21T01:52:26Z - Wave 2 policy-bound approval lane merged
- What was implemented
  - Closed `AF2-POL-003` and `AF2-POL-004` via PR #490.
  - Replaced proofed-mode ad hoc approval-token semantics with signed human approval receipts and bound approval/data-handling verification to the effective signed policy hash.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-core`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/policy-binding.test.ts test/wrap-quiet.test.ts`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `npm test -- test/proof-bundle-data-handling.test.ts`
- Notes
  - Reviewer fixes closed an offline-verification replay gap by requiring embedded approval-receipt event bindings to belong to the bundle event chain.
  - Rebasing this lane after PR #489 required aligning approval-related tests with the richer `credential.password` DLP taxonomy while preserving signed policy evidence.
  - Wave 2 foundations are now in place for attestation-receipt issuance and later trust-tier uplift lanes.
---

## 2026-03-21T02:28:29Z - Wave 3 runner-attestation lane merged
- What was implemented
  - Closed `AF2-ATT-002` via PR #492.
  - Added signed runner attestation receipts bound to runner measurement metadata, event/run anchors, and effective signed policy hash, with fail-closed offline/service verification.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-core`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/policy-binding.test.ts test/wrap-policy-binding.test.ts`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `npm test -- test/policy-binding.test.ts`
- Notes
  - Reviewer fixes normalized offline/service missing-sibling behavior so `runner_measurement` and `runner_attestation_receipt` fail closed coherently as `MISSING_REQUIRED_FIELD` when not paired.
---

## 2026-03-21T02:36:18Z - Wave 3 DLP simulation lane merged
- What was implemented
  - Closed `AF2-DLP-002` via PR #493.
  - Added deterministic structured redaction operations and explicit simulated-vs-enforced DLP receipt semantics, including metadata counters and verifier component summaries.
- Validation
  - `packages/clawsig-sdk`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npx vitest run test/wrap-quiet.test.ts`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `npx vitest run test/proof-bundle-data-handling.test.ts`
- Notes
  - Reviewer fixes forced global matching for custom redact rules so repeated secrets are fully redacted even when the author omits the `g` flag.
  - Wave 3 now unlocks attested trust-tier uplift (`AF2-ATT-003`) as the next dependency-satisfied attestation lane.
---

## 2026-03-21T03:03:21Z - Wave 4 attested-tier lane merged
- What was implemented
  - Closed `AF2-ATT-003` via PR #495.
  - Taught verifier surfaces to grant `trust_tier: attested` only when runner attestation is present and valid, with deterministic non-grant reason signaling otherwise.
- Validation
  - `packages/clawverify-core`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm run typecheck && npm run build`
  - `packages/clawverify-cli`: `npm test -- test/attested-tier-runner-attestation.test.ts`
  - `services/clawverify`: `npm ci && npm run typecheck`
  - `services/clawverify`: `npm test -- test/policy-binding.test.ts test/proof-bundle-attestations.test.ts`
- Notes
  - Reviewer fixes ensured malformed runner-measurement / runner-attestation evidence reports `ATTESTED_TIER_NOT_GRANTED_INVALID_RUNNER_ATTESTATION` deterministically even on early fail-closed returns.
  - Wave 4 unlocks attestation posture UX (`AF2-ATT-004`) and marketplace assurance requirements (`AF2-MKT-001`) as the next dependency-cleared lanes.
---

## 2026-03-21T04:00:27Z - Wave 5 attestation posture lane merged
- What was implemented
  - Closed `AF2-ATT-004` via PR #497.
  - Extended `clawsig prove` / export report surfaces to present runner-attestation posture conservatively within the existing proof/privacy posture narrative, including deterministic attested-vs-non-attested reasons and extracted evidence files.
- Validation
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm test`
- Notes
  - Reviewer fixes tightened runner measurement / attestation parsing so prove/export cannot over-claim attested posture from shallow metadata, and export packs now include extracted attestation evidence files when present.
---

## 2026-03-21T04:07:14Z - Wave 5 marketplace requirement lane merged
- What was implemented
  - Closed `AF2-MKT-001` via PR #498.
  - Added strict machine-readable bounty/task assurance requirements, surfaced them in worker list/claim flows before acceptance/submission, and made malformed or unsupported requirement sets fail closed in both service and CLI parsing.
- Validation
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npm test`
  - `services/clawverify`: `npm run typecheck`
  - `services/clawverify`: `npm test -- test/schema-runtime-contract.test.ts`
  - `services/clawverify`: `SCHEMA_DRIFT_BASE=main npm run check:schema-runtime-sync`
- Notes
  - Reviewer fixes corrected worker-facing list parsing so nested reward/proof-tier contract details and actual processor identifiers remain visible before acceptance.
  - A post-CI follow-up added `schema-runtime-contract` coverage for the new assurance schemas so the clawverify schema drift gate remains fail-closed when marketplace schema contracts evolve.
  - Wave 5 unlocks marketplace submission gating (`AF2-MKT-002`) as the next direct dependency-cleared lane.
---

## 2026-03-21T04:26:01Z - Wave 6 reviewer viewer lane merged
- What was implemented
  - Closed `AF2-REV-001` via PR #500.
  - Extended `clawsig prove --export-pack` to emit a portable local/hosted reviewer surface (`viewer/index.html`) plus pre-rendered report HTML while reusing the existing proof/privacy/attestation posture narrative.
- Validation
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npm test`
- Notes
  - Reviewer fixes hardened export-pack path validation so manifest entries, file writes, and viewer artifact links all fail closed on unsafe paths and remain deterministic.
---

## 2026-03-21T04:38:44Z - Wave 6 marketplace submission-gating lane merged
- What was implemented
  - Closed `AF2-MKT-002` via PR #501.
  - Enforced bounty/task assurance requirements at submission time, added deterministic reject/quarantine reason codes and reviewer-visible structured gating details, and reconciled quarantined submissions after proof verification completes.
- Validation
  - `services/_archived/clawbounties`: `npm run typecheck`
  - `git diff --check`
- Notes
  - Reviewer fixes made proof-backed privacy/processor/approval gates fail closed while proof verification is still pending and added automatic reconciliation so quarantined submissions can advance into review once verified evidence satisfies the contract.
  - Wave 6 unlocks payout/review routing by assurance tier (`AF2-MKT-003`) and side-by-side review flows (`AF2-REV-002`, `AF2-REV-003`) as the next dependency-cleared lanes.
---

## 2026-03-21T05:02:19Z - Wave 7 payout-routing lane merged
- What was implemented
  - Closed `AF2-MKT-003` via PR #503.
  - Added a unified payout/review policy evaluator that routes or blocks payout/readiness based on assurance gating, proof/commit verification, submission status, closure type, and minimum proof-tier requirements.
- Validation
  - `services/_archived/clawbounties`: `npm run typecheck`
  - `git diff --check`
- Notes
  - Reviewer fixes added deterministic minimum-proof-tier payout blockers even without explicit assurance-level requirements and carried payout-review policy through next-action surfaces so operators see the true wait/gate reason.
---

## 2026-03-21T05:06:45Z - Wave 7 run-comparison lane merged
- What was implemented
  - Closed `AF2-REV-002` via PR #504.
  - Added `clawsig prove --compare-with` plus portable export-pack comparison artifacts and viewer/report deltas across assurance, evidence, policy, processor, and privacy categories.
- Validation
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm test`
- Notes
  - Reviewer fixes removed wall-clock nondeterminism from comparison artifacts and made reviewer highlights category-aware so evidence-only or policy-only deltas do not collapse into a misleading no-delta message.
  - Wave 7 leaves reviewer signoff/dispute binding (`AF2-REV-003`) and later transparency/revocation lanes as the next reviewer-surface dependency chain.
---

## 2026-03-21T05:34:35Z - Wave 8 Python parity lane merged
- What was implemented
  - Closed `AF2-XRT-001` via PR #506.
  - Added proofed Python adapter parity for `clawverify wrap`, including injected `sitecustomize.py` bootstrap enforcement, verifier-compatible receipt/policy/attestation binding, and an end-to-end Python proofed-path test.
- Validation
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npx vitest run test/wrap-python-proofed.test.ts`
  - `packages/clawverify-cli`: `npm test`
- Notes
  - Reviewer fixes made proofed Python runs fail closed on interpreter flags (`-S`, `-I`, `-E`, `--isolated`) that would bypass the injected adapter and silently disable egress guarding.
---

## 2026-03-21T10:17:37Z - Wave 8 reviewer signoff lane fully hardened
- What was implemented
  - Closed `AF2-REV-003` via PR #507 plus hardening follow-up PR #508.
  - Added signed reviewer signoff receipts with dispute-note/evidence binding, fail-closed verifier enforcement in both core/service verifiers, and coherent prove/export/viewer/report surfacing.
- Validation
  - `packages/clawverify-core`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm test -- test/prove-cmd.test.ts test/reviewer-signoff-verifier.test.ts`
  - `services/clawverify`: `npm run typecheck`
  - `services/clawverify`: `npm test -- test/reviewer-signoff.test.ts`
- Notes
  - Post-merge review found the CLI/reporting path could overstate malformed or unbound reviewer receipts as structured evidence. PR #508 hardened local summarization to require the same signature/binding integrity expectations that the verifiers already enforced.
  - Wave 8 leaves browser/automation parity (`AF2-XRT-002`) and transparency/revocation (`AF2-TRN-001`/`002`) as the remaining major framework lanes.
---

## 2026-03-21T10:59:35Z - Wave 9 browser parity lane merged
- What was implemented
  - Closed `AF2-XRT-002` via PR #510.
  - Added browser/automation proofed adapter parity so browser-named runners inherit clawsig-managed proxy mediation, proofed env propagation, fail-closed bypass checks, and end-to-end verifier-compatible evidence flow.
- Validation
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawsig-sdk`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm test -- test/wrap-browser-proofed.test.ts test/wrap-python-proofed.test.ts`
- Notes
  - Reviewer fixes moved bypass-flag rejection ahead of long-lived proxy startup, expanded fail-closed browser flag coverage beyond `--no-proxy-server`, and tightened browser proof-path assertions so silent proxy downgrades are caught.
---

## 2026-03-21T11:06:27Z - Wave 9 transparency-log lane merged
- What was implemented
  - Closed `AF2-TRN-001` via PR #511.
  - Added optional transparency-log anchor metadata for policy, runner attestation, and reviewer signoff receipts, with fail-closed inclusion/consistency verification when anchor metadata is present.
- Validation
  - `services/clawverify`: `npm run typecheck`
  - `packages/clawverify-core`: `npm run typecheck`
  - `services/clawverify`: `npm test -- --run test/policy-binding.test.ts test/reviewer-signoff.test.ts`
  - `packages/clawverify-core`: `npm run build`
  - `packages/clawverify-cli`: `npm run build`
  - `packages/clawverify-cli`: `npm run test:proof-reliability`
- Notes
  - Post-CI follow-up rebased the lane after PR #510 merged, reran proof-reliability guardrails locally, refreshed the DID proof, and force-pushed with lease before final merge.
  - Reviewer fixes tightened same-tree consistency semantics and rejected malformed transparency `anchor_uri` values fail-closed.
  - Wave 9 leaves revocation (`AF2-TRN-002`) as the final remaining Framework vNext lane.
---

## 2026-03-21T17:31:20Z - Wave 10 revocation lane merged
- What was implemented
  - Closed `AF2-TRN-002` via PR #513.
  - Added revocation artifacts for policy-issuer, runner, and reviewer trust roots, with deterministic verifier rejection and conservative prove/export/report surfacing for revocation-driven failures.
- Validation
  - `packages/clawverify-core`: `npm run typecheck`
  - `packages/clawverify-core`: `npm run build`
  - `services/clawverify`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run typecheck`
  - `packages/clawverify-cli`: `npm run build`
  - `services/clawverify`: `npm test -- test/policy-binding.test.ts test/reviewer-signoff.test.ts`
  - `packages/clawverify-cli`: `npm test -- test/policy-binding.test.ts test/reviewer-signoff-verifier.test.ts test/prove-cmd.test.ts`
- Notes
  - Reviewer fixes ensured nested runner/reviewer receipt revocation is enforced only after nested payload/signature authentication, preventing tampered receipts from over-claiming `REVOKED`.
  - With revocation shipped, Framework vNext is complete: all 24 planned stories are now merged.
---
