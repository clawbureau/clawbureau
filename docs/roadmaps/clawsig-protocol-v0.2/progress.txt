2026-02-17 — docs normalization lane (CPL-V2-002/003/004/005 pre-implementation)

- Integrated normalized reason-code registry deltas for co-sign, disclosure, aggregate, and temporal guardrail semantics in:
  - docs/specs/clawsig-protocol/REASON_CODE_REGISTRY.md
- Added canonical schema-ID namespace policy and explicit legacy v1 tool alias handling in:
  - docs/specs/clawsig-protocol/CLAWSIG_PROTOCOL_v1.0.md
- Locked fail-closed unknown schema behavior:
  - UNKNOWN_SCHEMA_ID remains mandatory for unrecognized IDs.

Notes:
- This lane is docs-only and architecture-preserving.
- Runtime/schema/conformance implementation follows in subsequent CPL-V2 execution lanes.

---

2026-02-17 — schema foundations lane (CPL-V2-005/006/010)

- Added additive R48/R49 schema foundations under `packages/schema/poh/`:
  - `selective_disclosure.v1.json`
  - `co_signature.v1.json`
  - `tool_receipt.v2.json`
  - `tool_receipt_envelope.v2.json`
  - `aggregate_bundle.v1.json`
  - `aggregate_bundle_envelope.v1.json`
- Extended `proof_bundle.v1.json` for additive compatibility:
  - `tool_receipts[]` now accepts finite, explicit v1/v2 payload/envelope schema set
  - receipt collection arrays bounded (`maxItems: 1000`) for deterministic verification limits
  - inline receipt envelope surfaces now allow optional `expires_at`
- Added optional `expires_at` to envelope schemas used by proof/export paths:
  - `proof_bundle_envelope.v1.json`, `export_bundle.v1.json`, and receipt envelopes
- Implemented fail-closed schema-ID normalization plumbing in both runtime registries:
  - `services/clawverify/src/schema-registry.ts`
  - `packages/clawverify-core/src/schema-registry.ts`
  - Explicit finite alias map for legacy v1 tool URIs; unknown IDs remain `UNKNOWN_SCHEMA_ID`.
- Regenerated schema validators (service + core) with new schema coverage and updated drift checks.

Validation:
- `services/clawverify`: `npm run check:schema-runtime-sync`, `npm run typecheck`, `npm test` ✅
- `packages/clawverify-core`: `npm run typecheck` ✅

---

2026-02-17 — verifier R48 lane (CPL-V2-002/004 runtime enforcement)

- Upgraded `packages/clawverify-core/src/verify-proof-bundle.ts` tool receipt path from legacy v1-only checks to finite union handling:
  - payload v1
  - envelope v1
  - payload v2
  - envelope v2
- Added deterministic R48 fail-closed checks in core verifier:
  - co-signature cascade verification (`CO_SIGNATURE_INVALID` on any failing co-signature)
  - selective disclosure validation (`DISCLOSURE_ALGORITHM_UNKNOWN`, `DISCLOSURE_TYPE_MISMATCH`, `DISCLOSURE_ROOT_MISMATCH`)
  - tool-receipt binding checks against bundle run/event context (`RECEIPT_BINDING_MISMATCH`)
  - envelope TTL enforcement for tool receipt envelopes (`EXPIRED_TTL`)
- Extended core schema-validation wrappers for newly generated validators:
  - `validateToolReceipt{V1,V2}`
  - `validateToolReceiptEnvelope{V1,V2}`
  - `validateSelectiveDisclosureV1`
  - `validateCoSignatureV1`
- Updated core type surface for additive schema support:
  - new tool receipt v2 / envelope v2 / co-signature / selective-disclosure interfaces
  - `ProofBundlePayload.tool_receipts` widened to v1/v2 payload+envelope union
- Updated `trace-compiler` to handle envelope-backed tool receipt entries without breaking deterministic trace compilation.

Validation:
- `cd packages/clawverify-core && npm run typecheck` ✅
- `cd packages/clawverify-core && npm run build` ✅
- `cd packages/clawverify-cli && npm run build` ✅
- `node --experimental-strip-types --no-warnings --test packages/clawverify-cli/test/cross-platform.test.ts` ✅
- `node scripts/protocol/run-clawsig-protocol-conformance.mjs` ✅

---

2026-02-17 — verifier R49 lane (aggregate + temporal guardrails)

- Added new aggregate verifier in core:
  - `packages/clawverify-core/src/verify-aggregate-bundle.ts`
  - deterministic fail-closed checks for:
    - signer/issuer binding (`AGGREGATE_SIGNER_MISMATCH`)
    - temporal causality/future poisoning/TTL (`CAUSAL_CLOCK_CONTRADICTION`, `FUTURE_TIMESTAMP_POISONING`, `EXPIRED_TTL`)
    - member sorting + anti-replay dedupe (`UNSORTED_MEMBER_ARRAY`, `AGGREGATE_DUPLICATE_MEMBER`, `AGGREGATE_DUPLICATE_BUNDLE_ID`, `AGGREGATE_DUPLICATE_RUN_ID`)
    - strict-liability member cascade (`AGGREGATE_MEMBER_INVALID`)
    - aggregate/member TTL coherence (`AGGREGATE_TTL_EXCEEDS_MEMBER`)
    - aggregate issuer/member identity conflict (`IDENTITY_CONFLICT`)
    - fleet summary reconciliation (`FLEET_SUMMARY_MISMATCH`)
- Extended core exports/types for aggregate runtime:
  - `packages/clawverify-core/src/types.ts`
  - `packages/clawverify-core/src/index.ts`
- Extended schema-validation wrappers with aggregate schema validators:
  - `validateAggregateBundleV1`
  - `validateAggregateBundleEnvelopeV1`
- Hardened export-bundle verifier for R49 temporal + orphan-attachment constraints:
  - `verifyAt` / `ttlSkewMs` support
  - `expires_at` included in bundle hash view
  - temporal checks (`EXPIRED_TTL`, `CAUSAL_CLOCK_CONTRADICTION`, `FUTURE_TIMESTAMP_POISONING`)
  - derivation/audit `execution.proof_bundle_hash_b64u` mismatch guard (`HASH_MISMATCH`)
- Added CLI/runtime wiring for aggregate verification without changing manifest contract:
  - `clawverify verify export-bundle` now auto-dispatches aggregate envelopes to `verifyAggregateBundle`
  - alias support: `clawverify verify aggregate-bundle`
  - conformance runner accepts `kind: aggregate_bundle` mapping to `aggregate-bundle`
- Added reason-code hint coverage for new R48/R49 codes in:
  - `packages/clawverify-cli/src/hints.ts`

Validation:
- `cd packages/clawverify-core && npm run typecheck` ✅
- `cd packages/clawverify-core && npm run build` ✅
- `cd packages/clawverify-cli && npm run build` ✅
- `node --experimental-strip-types --no-warnings --test packages/clawverify-cli/test/cross-platform.test.ts` ✅
- `node scripts/protocol/run-clawsig-protocol-conformance.mjs` ✅
- `node scripts/protocol/run-clawsig-verified-pr.mjs` ✅

---

2026-02-17 — fixtures + conformance lane (CPL-V2 R48/R49 executable vectors)

- Added concrete R48 proof-bundle vectors under:
  - `packages/schema/fixtures/protocol-conformance/r48-r49-v02/proof_bundle/`
  - coverage includes:
    - v2 co-signature pass/fail (`OK`, `CO_SIGNATURE_INVALID`)
    - selective disclosure fail paths (`DISCLOSURE_TYPE_MISMATCH`, `DISCLOSURE_ROOT_MISMATCH`)
    - v2 tool receipt binding replay fail (`RECEIPT_BINDING_MISMATCH`)
- Added concrete R49 aggregate vectors under:
  - `packages/schema/fixtures/protocol-conformance/r48-r49-v02/aggregate_bundle/`
  - coverage includes:
    - pass golden (`OK`)
    - signer mismatch (`AGGREGATE_SIGNER_MISMATCH`)
    - ordering invariant (`UNSORTED_MEMBER_ARRAY`)
    - duplicate run replay (`AGGREGATE_DUPLICATE_RUN_ID`)
    - strict-liability child failure (`AGGREGATE_MEMBER_INVALID`)
    - fleet summary mismatch (`FLEET_SUMMARY_MISMATCH`)
    - self-dealing identity conflict (`IDENTITY_CONFLICT`)
- Updated protocol conformance manifest contract with executable R48/R49 vectors:
  - `packages/schema/fixtures/protocol-conformance/manifest.v1.json`
  - protocol header moved to `0.2`
  - added `kind: aggregate_bundle` vectors (supported by runner/CLI mapping)

Validation:
- `node scripts/protocol/run-clawsig-protocol-conformance.mjs` ✅
- `node --experimental-strip-types --no-warnings --test packages/clawverify-cli/test/cross-platform.test.ts` ✅
- `node scripts/protocol/run-clawsig-verified-pr.mjs` ✅

---

2026-02-18 — CPL-V2-001 rate-limit semantics lane (schema + verifier + conformance)

- Extended additive proof bundle schema with deterministic rate-limit claims:
  - `packages/schema/poh/proof_bundle.v1.json`
  - new optional `rate_limit_claims[]` entries (scope/window/max/observed requests + token counters)
- Implemented fail-closed proof-bundle checks in core + service verifiers:
  - `RATE_LIMIT_WINDOW_INVALID` for `window_start > window_end`
  - `RATE_LIMIT_CLAIM_INCONSISTENT` for malformed paired fields/run-id mismatches
  - `RATE_LIMIT_EXCEEDED` when observed usage exceeds declared maxima
- Implemented aggregate cross-bundle checks in core verifier:
  - per agent/scope/window claim consistency reconciliation
  - deterministic burst detection by summing observed counts across members
- Added reason-code documentation and CLI hints for new rate-limit reason codes.
- Added executable conformance vectors under:
  - `packages/schema/fixtures/protocol-conformance/cpl-v2-rate-limit/`
    - proof bundle pass + fail cases
    - aggregate bundle inconsistent + exceeded cases
  - wired in `packages/schema/fixtures/protocol-conformance/manifest.v1.json`
- Regenerated core/service AJV standalone validators after schema update.

Validation:
- `cd packages/clawverify-core && npm run typecheck && npm run build` ✅
- `cd packages/clawverify-cli && npm run build` ✅
- `cd services/clawverify && npm run typecheck && npm test` ✅
- `node services/clawverify/scripts/check-schema-runtime-sync.mjs` ✅
- `node scripts/protocol/run-clawsig-protocol-conformance.mjs` ✅
- `node --experimental-strip-types --no-warnings --test packages/clawverify-cli/test/cross-platform.test.ts` ✅
- `node scripts/protocol/run-clawsig-verified-pr.mjs` ✅

---

2026-02-18 — v0.2 release notes + tracker closeout

- Final CPL-V2-001 lane merged:
  - PR #285 (`1b21d0cdb562e02347304451cc6edd137a5b99d2`)
- Tracker status updated to fully shipped on main:
  - `docs/roadmaps/clawsig-protocol-v0.2/prd.json`
  - `docs/roadmaps/clawsig-protocol-v0.2/README.md`
  - `docs/roadmaps/README.md`
- Added release-grade protocol changelog entry:
  - `docs/specs/clawsig-protocol/CHANGELOG.md` (`v0.2.0`)
  - covers merged tranche PRs #281/#282/#283/#285 and behavior deltas.

Validation:
- `jq . docs/roadmaps/clawsig-protocol-v0.2/prd.json` ✅
- `node scripts/docs/lint-prds.mjs` ✅
