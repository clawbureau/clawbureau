2026-02-17 — docs normalization lane (CPL-V2-002/003/004/005 pre-implementation)

- Integrated normalized reason-code registry deltas for co-sign, disclosure, aggregate, and temporal guardrail semantics in:
  - docs/specs/clawsig-protocol/REASON_CODE_REGISTRY.md
- Added canonical schema-ID namespace policy and explicit legacy v1 tool alias handling in:
  - docs/specs/clawsig-protocol/CLAWSIG_PROTOCOL_v1.0.md
- Locked fail-closed unknown schema behavior:
  - UNKNOWN_SCHEMA_ID remains mandatory for unrecognized IDs.

Notes:
- This lane is docs-only and architecture-preserving.
- Runtime/schema/conformance implementation follows in subsequent CPL-V2 execution lanes.

---

2026-02-17 — schema foundations lane (CPL-V2-005/006/010)

- Added additive R48/R49 schema foundations under `packages/schema/poh/`:
  - `selective_disclosure.v1.json`
  - `co_signature.v1.json`
  - `tool_receipt.v2.json`
  - `tool_receipt_envelope.v2.json`
  - `aggregate_bundle.v1.json`
  - `aggregate_bundle_envelope.v1.json`
- Extended `proof_bundle.v1.json` for additive compatibility:
  - `tool_receipts[]` now accepts finite, explicit v1/v2 payload/envelope schema set
  - receipt collection arrays bounded (`maxItems: 1000`) for deterministic verification limits
  - inline receipt envelope surfaces now allow optional `expires_at`
- Added optional `expires_at` to envelope schemas used by proof/export paths:
  - `proof_bundle_envelope.v1.json`, `export_bundle.v1.json`, and receipt envelopes
- Implemented fail-closed schema-ID normalization plumbing in both runtime registries:
  - `services/clawverify/src/schema-registry.ts`
  - `packages/clawverify-core/src/schema-registry.ts`
  - Explicit finite alias map for legacy v1 tool URIs; unknown IDs remain `UNKNOWN_SCHEMA_ID`.
- Regenerated schema validators (service + core) with new schema coverage and updated drift checks.

Validation:
- `services/clawverify`: `npm run check:schema-runtime-sync`, `npm run typecheck`, `npm test` ✅
- `packages/clawverify-core`: `npm run typecheck` ✅

---

2026-02-17 — verifier R48 lane (CPL-V2-002/004 runtime enforcement)

- Upgraded `packages/clawverify-core/src/verify-proof-bundle.ts` tool receipt path from legacy v1-only checks to finite union handling:
  - payload v1
  - envelope v1
  - payload v2
  - envelope v2
- Added deterministic R48 fail-closed checks in core verifier:
  - co-signature cascade verification (`CO_SIGNATURE_INVALID` on any failing co-signature)
  - selective disclosure validation (`DISCLOSURE_ALGORITHM_UNKNOWN`, `DISCLOSURE_TYPE_MISMATCH`, `DISCLOSURE_ROOT_MISMATCH`)
  - tool-receipt binding checks against bundle run/event context (`RECEIPT_BINDING_MISMATCH`)
  - envelope TTL enforcement for tool receipt envelopes (`EXPIRED_TTL`)
- Extended core schema-validation wrappers for newly generated validators:
  - `validateToolReceipt{V1,V2}`
  - `validateToolReceiptEnvelope{V1,V2}`
  - `validateSelectiveDisclosureV1`
  - `validateCoSignatureV1`
- Updated core type surface for additive schema support:
  - new tool receipt v2 / envelope v2 / co-signature / selective-disclosure interfaces
  - `ProofBundlePayload.tool_receipts` widened to v1/v2 payload+envelope union
- Updated `trace-compiler` to handle envelope-backed tool receipt entries without breaking deterministic trace compilation.

Validation:
- `cd packages/clawverify-core && npm run typecheck` ✅
- `cd packages/clawverify-core && npm run build` ✅
- `cd packages/clawverify-cli && npm run build` ✅
- `node --experimental-strip-types --no-warnings --test packages/clawverify-cli/test/cross-platform.test.ts` ✅
- `node scripts/protocol/run-clawsig-protocol-conformance.mjs` ✅
