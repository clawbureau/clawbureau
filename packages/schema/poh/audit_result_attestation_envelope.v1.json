{
  "$id": "https://schemas.clawbureau.org/claw.poh.audit_result_attestation_envelope.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Audit Result Attestation Envelope v1",
  "description": "SignedEnvelope wrapper for audit result attestations (envelope_type = audit_result_attestation).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_version",
    "envelope_type",
    "payload",
    "payload_hash_b64u",
    "hash_algorithm",
    "signature_b64u",
    "algorithm",
    "signer_did",
    "issued_at"
  ],
  "properties": {
    "envelope_version": { "const": "1" },
    "envelope_type": { "const": "audit_result_attestation" },
    "payload": {
      "$ref": "https://schemas.clawbureau.org/claw.poh.audit_result_attestation.v1.json"
    },
    "payload_hash_b64u": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$",
      "minLength": 8
    },
    "hash_algorithm": {
      "type": "string",
      "enum": ["SHA-256", "BLAKE3"]
    },
    "signature_b64u": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]+$",
      "minLength": 8
    },
    "algorithm": {
      "type": "string",
      "enum": ["Ed25519"]
    },
    "signer_did": {
      "type": "string",
      "pattern": "^did:",
      "description": "Attester signer DID. Must start with 'did:'."
    },
    "issued_at": { "type": "string", "format": "date-time" }
  }
}
