{
  "project": "clawbureau",
  "branchName": "feat/schema/AEM-US-001-schema-v2-usd-minor",
  "description": "packages/schema — Agent Economy MVP schema alignment (USD minor units + proof tier terminology).",
  "userStories": [
    {
      "id": "AEM-US-001",
      "title": "Schema v2: USD minor units + terminology normalization",
      "description": "As an integrator, I want v2 schemas using USD minor-unit strings and proof-tier naming so that services and OpenClaw integrations share deterministic contracts.",
      "acceptanceCriteria": [
        "New v2 schemas added with new $id values; strict additionalProperties:false",
        "All monetary fields are string integers in minor units",
        "Add min_proof_tier enum (self|gateway|sandbox) and mark min_poh_tier as legacy",
        "Schemas are valid JSON (draft 2020-12)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Added v2 schema files for bounties + escrow using USD-only currency and integer-string minor-unit fields (e.g. amount_minor). Introduced min_proof_tier enum (self|gateway|sandbox) and marked legacy min_poh_tier as deprecated."
    },
    {
      "id": "AEM-US-002",
      "title": "Identity rotation schema + golden vector fixture",
      "description": "As a verifier/integrator, I want a standard DID rotation certificate schema and an authoritative golden vector fixture so identity primitives are testable and cross-implementation compatible.",
      "acceptanceCriteria": [
        "Add packages/schema/identity/did_rotation.v1.json with new $id and strict additionalProperties:false",
        "Add packages/schema/fixtures/protocol-m-golden-vector.v1.json as a stable test vector reference",
        "Update packages/schema/README.md to list the new identity schema",
        "JSON files are valid (draft 2020-12 for schema)"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Added did_rotation v1 schema and imported the Protocol M golden vector as a fixture for automated verification tests."
    },
    {
      "id": "AEM-US-003",
      "title": "PoH envelope schemas for strict verification",
      "description": "As a verifier/integrator, I want explicit envelope + payload schemas for proof bundles and gateway receipts so services can run strict Ajv validation and fail closed on unknown fields.",
      "acceptanceCriteria": [
        "Add packages/schema/poh/gateway_receipt.v1.json payload schema",
        "Add packages/schema/poh/gateway_receipt_envelope.v1.json envelope schema",
        "Add packages/schema/poh/proof_bundle_envelope.v1.json envelope schema",
        "Update proof_bundle.v1.json to reference receipt_binding.v1.json for binding strictness",
        "Update packages/schema/README.md to list the new schemas",
        "Schemas are valid JSON (draft 2020-12)"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Added explicit PoH schemas for gateway receipts and proof bundle envelopes and tightened receipt.binding validation by referencing receipt_binding.v1.json."
    },
    {
      "id": "CRP-OPS-001-SCHEMA",
      "title": "Reputation schema contracts for clawrep MVP",
      "description": "As trust services, we need strict versioned reputation schemas for ingest/profile/reviewer contracts.",
      "acceptanceCriteria": [
        "Add reputation_event_ingest.v1.json with strict closure ingest fields",
        "Add reputation_profile.v1.json for deterministic profile/tier output",
        "Add reviewer selection request/response schemas",
        "Update packages/schema/README.md listings"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Added packages/schema/reputation/{reputation_event_ingest,reputation_profile,reviewer_selection_request,reviewer_selection_response}.v1.json and updated README listings."
    },
    {
      "id": "CRP-US-008-SCHEMA",
      "title": "Closed-loop reputation envelope + anti-collusion schema extensions",
      "description": "As trust services, we need strict schema contracts for ingest-loop envelopes and reviewer anti-collusion metadata while preserving deterministic compatibility.",
      "acceptanceCriteria": [
        "Add reputation_event_loop_envelope.v1.json for closure|penalty|recovery canonical event envelopes",
        "Extend reviewer selection request schema with requester_did/worker_did optional fields",
        "Extend reviewer selection response schema with deterministic selection_metadata payload",
        "Update packages/schema/README.md reputation listings"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Added `reputation_event_loop_envelope.v1.json`, extended reviewer request/response schemas for anti-collusion context/metadata, and updated schema README listings."
    }
  ]
}
