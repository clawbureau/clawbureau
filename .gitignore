# deps
node_modules

# build artifacts
**/dist/

# env
.env

# OS
.DS_Store

# pi / ralph artifacts
.pi/
.ralph-tui/
.ralph-output/
.cx/
.last-branch
ralph.out
ralph.pid
fleet.out
fleet*.out

# NOTE: we want to keep a docs-level archive tracked, so only ignore generic archive dirs
archive/
!docs/archive/
!docs/archive/**

# locks (prefer pnpm/yarn if needed; avoid committing npm locks from agent runs)
package-lock.json
!packages/bounties/package-lock.json
!packages/clawverify-core/package-lock.json
!packages/clawverify-cli/package-lock.json
!services/clawproxy/package-lock.json
!services/clawverify/package-lock.json
!services/claw-domains/package-lock.json

# local scratch
.tmp-main-required-pr-reviews.json

# clawsig local keys / scratch output (do not commit)
.clawsig-key.json
.clawsig/
# Legacy clawproof names (transition compat)
.clawproof-key.json
.clawproof/

# hackathon / analysis scratch
hackathons/**/analysis/

# wrangler local state
.wrangler/
.dev.vars
wrangler-state/
