{
  "name": "@clawbureau/clawrep",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical reputation service for deterministic reviewer selection and trust tiers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.105.0"
  }
}
