name: clawverify-firewall-conformance

on:
  pull_request:
    paths:
      - 'services/clawverify/**'
      - 'packages/schema/fixtures/protocol-conformance/clawverify-firewall/**'
      - 'scripts/protocol/run-clawverify-firewall-conformance.mjs'
      - '.github/workflows/clawverify-firewall-conformance.yml'
  push:
    branches:
      - main
    paths:
      - 'services/clawverify/**'
      - 'packages/schema/fixtures/protocol-conformance/clawverify-firewall/**'
      - 'scripts/protocol/run-clawverify-firewall-conformance.mjs'
      - '.github/workflows/clawverify-firewall-conformance.yml'

jobs:
  firewall-conformance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clawverify deps
        run: |
          cd services/clawverify
          npm ci

      - name: Run firewall conformance fixtures
        run: |
          node scripts/protocol/run-clawverify-firewall-conformance.mjs
