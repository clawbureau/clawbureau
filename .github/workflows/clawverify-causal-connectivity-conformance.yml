name: clawverify-causal-connectivity-conformance

on:
  pull_request:
    paths:
      - 'services/clawverify/**'
      - 'packages/schema/fixtures/protocol-conformance/clawverify-causal-connectivity/**'
      - 'services/clawverify/test/firewall-conformance.test.ts'
      - 'scripts/protocol/run-clawverify-causal-connectivity-conformance.mjs'
      - '.github/workflows/clawverify-causal-connectivity-conformance.yml'
  push:
    branches:
      - main
    paths:
      - 'services/clawverify/**'
      - 'packages/schema/fixtures/protocol-conformance/clawverify-causal-connectivity/**'
      - 'services/clawverify/test/firewall-conformance.test.ts'
      - 'scripts/protocol/run-clawverify-causal-connectivity-conformance.mjs'
      - '.github/workflows/clawverify-causal-connectivity-conformance.yml'

jobs:
  causal-connectivity-conformance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clawverify deps
        run: |
          cd services/clawverify
          npm ci

      - name: Run causal connectivity conformance fixtures
        run: |
          node scripts/protocol/run-clawverify-causal-connectivity-conformance.mjs
